@model CanchasGambeta.ViewModels.NuevaReservaVM
@{
    ViewBag.Title = "MisReservas";
    List<SelectListItem> canchas = (List<SelectListItem>)ViewBag.canchas;
    List<SelectListItem> horarios = (List<SelectListItem>)ViewBag.horarios;
    List<SelectListItem> insumos = (List<SelectListItem>)ViewBag.insumos;
}

<div class="container">
    <div class="row" style="transform: translateY(60px);">
        <div class="col-md-6">
            <div>
                <h3 class="text-center" style="color:white">Mis reservas</h3>
            </div>
        </div>
    </div>
</div>

<div class="row" style="transform: translateY(60px); color:white">
    <div class="col-md-6">
        <!--<table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th class="text-center">
                    Integrantes
                </th>
                <th class="text-center acciones" style="display:none">
                    Acciones
                </th>
            </tr>
        </thead>
        <tbody>
            foreach (var listado in Model)
            {
                <tr class="text-center">
                    <td class="miembro">
                        Html.HiddenFor(Model => listado.IdEmail)
                        listado.Email
                    </td>
                    <td class="text-center acciones" style="display:none">
                        <div class="btn-group">
                            <a href="Url.Action("ModificarIntegrante", "Equipo", new {idEmail = listado.IdEmail, email1 = listado.Email})" class="btn btn-warning m-1">Modificar</a>
                            <a href="Url.Action("EliminarIntegrante", "Equipo", new { idEmail = listado.idEmail, email1 = listado.Email})" class="btn btn-danger m-1">Eliminar</a>
                        </div>
                    </td>
                </tr>

            }
        </tbody>
    </table>-->
    </div>
    <div class="col-md-6">
        <h4 class="p-1">Llene los siguientes campos para concretar una reserva:</h4>
        @using (Html.BeginForm("NuevaReserva", "Reserva", FormMethod.Post, new { onsubmit = "return validacionNuevaReserva();" }))
        {
            <div class="row">
                <div class="col-lg-12">
                    <div class="form-horizontal">
                        <div class="form-group">
                            <label class="col-sm-6 control-label">Fecha</label>
                            <div class="col-sm-6">
                                @Html.TextBoxFor(model => model.Fecha, new { @type = "date", @class = "form-control datepicker" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-6 control-label">Cancha</label>
                            <div class="col-sm-6">
                                @Html.DropDownList("idCancha", canchas, "Seleccione una cancha", htmlAttributes: new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="form-group">
                            <label class="col-sm-6 control-label">Horario</label>
                            <div class="col-sm-6">
                                @Html.DropDownList("idHorario", horarios, "Seleccione el horario", htmlAttributes: new { @class = "form-control" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-6">
                                <label class="control-label">Servicio asador</label>
                                @Html.CheckBoxFor(Model => Model.ServicioAsador, false)
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-6">
                                <label class="control-label">Servicio instrumentos</label>
                                @Html.CheckBoxFor(Model => Model.ServicioInstrumento, false)
                            </div>
                        </div>
                        <div class="form-group text-center">
                            <div class="col-xs-12">
                                <button class="btn btn-primary" type="submit">
                                    Registrar reserva
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="form-label-group text-danger" style="margin-left:10px">
                @ViewBag.ErrorInsertReserva
            </div>
        }
    </div>
</div>

